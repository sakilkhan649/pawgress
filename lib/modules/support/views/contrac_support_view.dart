import 'package:flutter/material.dart';

class ContractSupportView extends StatelessWidget {
  const ContractSupportView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}